<%= form_with(model: pokemon, local: true) do |form| %>
  <% if pokemon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pokemon.errors.count, "error") %> prohibited this pokemon from being saved:</h2>

      <ul>
      <% pokemon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.number_field :id, id: :pokemon_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :pokemon_name %>
  </div>

  <div class="field">
    <%= form.label :species_id %>
    <%= form.number_field :species_id, id: :pokemon_species_id %>
  </div>

  <div class="field">
    <%= form.label :height %>
    <%= form.number_field :height, id: :pokemon_height %>
  </div>

  <div class="field">
    <%= form.label :weight %>
    <%= form.number_field :weight, id: :pokemon_weight %>
  </div>

  <div class="field">
    <%= form.label :base_experience %>
    <%= form.number_field :base_experience, id: :pokemon_base_experience %>
  </div>

  <div class="field">
    <%= form.label :order %>
    <%= form.number_field :order, id: :pokemon_order %>
  </div>

  <div class="field">
    <%= form.label :is_default %>
    <%= form.check_box :is_default, id: :pokemon_is_default %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
