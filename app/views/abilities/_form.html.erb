<%= form_with(model: ability, local: true) do |form| %>
  <% if ability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ability.errors.count, "error") %> prohibited this ability from being saved:</h2>

      <ul>
      <% ability.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :identifier %>
    <%= form.text_field :identifier, id: :ability_identifier %>
  </div>

  <div class="field">
    <%= form.label :generation_id %>
    <%= form.text_field :generation_id, id: :ability_generation_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
